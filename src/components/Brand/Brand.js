import React from 'react'
import styles from './Brand.module.scss'

const Brand = () => (
  <div className={styles.brandLabel}>
    <span className={styles.brandTitle}>Global Hack</span>
  </div>
)

export default Brand
