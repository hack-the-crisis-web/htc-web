import React from 'react'
import styles from './FooterCategoryTitle.module.scss'

const FooterCategoryTitle = ({ title }) => (
  <div className={styles.title}>{title}</div>
)

export default FooterCategoryTitle
